# Chaos Cloth Editor Plugin Overview

## 1. What this plugin does

- Deprecated editor plugin for Chaos Cloth; superseded by the Chaos Cloth plugin and associated editors.
- Contains no active modules in this UE 5.7 build.

## 2. Key Modules

- No modules are defined in the `.uplugin` for this build; functionality has been replaced by newer Chaos Cloth tooling.

## 3. Important Types & APIs

- No public classes remain enabled in this plugin; use the `ChaosCloth` and `ChaosClothAssetEditor` plugins for cloth editing and simulation.

## 4. Typical usage patterns

- Do not rely on this plugin for current workflows. Enable the `ChaosCloth` and `ChaosClothAssetEditor` plugins instead.

## 5. Version-specific notes (UE 5.7)

- Marked deprecated in the plugin description for UE 5.7 and disabled by default.
